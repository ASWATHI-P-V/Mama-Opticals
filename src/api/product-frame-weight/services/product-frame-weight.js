'use strict';

/**
 * product-frame-weight service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::product-frame-weight.product-frame-weight');
