'use strict';

/**
 * product-frame-weight router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-frame-weight.product-frame-weight');
